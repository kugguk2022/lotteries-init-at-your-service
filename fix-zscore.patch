diff --git a/euromillions/roi.py b/euromillions/roi.py
index e814cde..807935d 100755
--- a/euromillions/roi.py
+++ b/euromillions/roi.py
@@ -903,6 +903,11 @@ def train_grok(
             )
     except Exception:
         pass  # optional output; ignore errors
+    return sig_full, best
+
+
+# --------------- RL Mixer ---------------
+def zscore(a, axis=1, eps=1e-12):
     m = a.mean(axis=axis, keepdims=True)
     s = a.std(axis=axis, keepdims=True)
     s = np.where(s < eps, 1.0, s)
diff --git a/euromillions_agent/lotto_lab.py b/euromillions_agent/lotto_lab.py
index e4c7d7a..4ddf781 100755
--- a/euromillions_agent/lotto_lab.py
+++ b/euromillions_agent/lotto_lab.py
@@ -847,6 +847,11 @@ def train_grok(
             )
     except Exception:
         pass  # optional output; ignore errors
+    return sig_full, best
+
+
+# --------------- RL Mixer ---------------
+def zscore(a, axis=1, eps=1e-12):
     m = a.mean(axis=axis, keepdims=True)
     s = a.std(axis=axis, keepdims=True)
     s = np.where(s < eps, 1.0, s)
